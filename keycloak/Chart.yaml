apiVersion: v2
name: keycloak
description: A Helm chart for Keycloak with custom fitness-devil theme
type: application
version: 0.1.0
appVersion: "26.0"

dependencies:
  - name: postgresql
    version: "15.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
